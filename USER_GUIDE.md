# 使用说明

1. Web 控制台无需登录；部署时由 nginx 反向代理从容器环境变量注入后端鉴权 Header，鉴权值不进入浏览器。直接调用 API 时使用运行环境配置的鉴权值。
2. 在 Agent 页面先点击“从接口拉取 Agent”，将上游数据更新到本地 SQLite。列表的“数据差异”可筛选本地修改、字段锁定、上游移除和仅本地 Agent；点击状态可查看与最近上游快照的字段级差异。
3. 可编辑名称、描述、正负语料、阈值与状态；AI 推荐只追加到编辑框，点击保存后才写入本地。
4. 确认本地内容后点击“同步到向量数据库”。普通保存不会隐式更新 Qdrant。
5. 在诊断页扫描语义冲突、查看 UMAP 点云、使用 AI 修复或合并 Agent；“保存、同步并重检”会显式更新相关向量。
6. 在路由测试页输入问题。所有达到阈值的 Agent 都会连同分数返回，并按分数降序排列；没有 Agent 达标时返回 `data/default_route.txt` 的文本。
7. 设置页可管理 Qdrant、Embedding、LLM 的非秘密参数、提示词和诊断阈值，保存后相关组件会延迟重连。服务凭据只由部署环境或 1Panel 注入，管理页面不会读取、展示或修改。

所有 Agent 编辑仅作用于本地 SQLite，不会回写上游 Agent API。
差异基准是最近一次成功拉取的上游快照，并非实时查询上游；人工覆盖字段不会被后续拉取覆盖，上游消失的 Agent 会转为停用。

## 未命中时的大模型兜底

设置页的“未命中时的大模型兜底”默认关闭。先配置非秘密模型参数、确认部署环境已提供 LLM 凭据，再完成一次增量同步，最后开启兜底。名称和描述应写清能完成的职责及不支持的操作。

普通路由有结果时仍返回全部合格 Agent。只有未命中才召回描述并调用模型；模型可以拒绝或报告歧义，超时／异常则保留原默认回复。测试页标注“大模型兜底”，不显示虚构相似度；歧义时提示补充对象或动作。关闭开关即可恢复默认处理，无需删除向量。

上线前应使用真实业务样本分别检查长尾正确命中、边界／域外拒绝、歧义及负例约束，并测量成本和延迟。离线程序测试与模拟界面不能证明真实模型效果。详见 [BUPT 交付记录](docs/changes/llm-fallback/README.md)。
