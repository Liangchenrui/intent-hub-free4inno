# 使用说明

1. Web 控制台无需登录，会自动使用固定鉴权码 `telestar`；直接调用 API 时发送 `Authorization: Bearer telestar`。
2. 在 Agent 页面先点击“从接口拉取 Agent”，将上游数据更新到本地 SQLite。列表的“数据差异”可筛选本地修改、字段锁定、上游移除和仅本地 Agent；点击状态可查看与最近上游快照的字段级差异。
3. 可编辑名称、描述、正负语料、阈值与状态；AI 推荐只追加到编辑框，点击保存后才写入本地。
4. 确认本地内容后点击“同步到向量数据库”。普通保存不会隐式更新 Qdrant。
5. 在诊断页扫描语义冲突、查看 UMAP 点云、使用 AI 修复或合并 Agent；“保存、同步并重检”会显式更新相关向量。
6. 在路由测试页输入问题。最高分达到阈值时返回 Agent 完整详情及分数，否则返回 `data/default_route.txt` 的文本。
7. 设置页可管理 Qdrant、Embedding、LLM、提示词和诊断阈值，保存后相关组件会延迟重连。

所有 Agent 编辑仅作用于本地 SQLite，不会回写上游 Agent API。
差异基准是最近一次成功拉取的上游快照，并非实时查询上游；人工覆盖字段不会被后续拉取覆盖，上游消失的 Agent 会转为停用。
